---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu, tag: "14.04"}

inputs:
- name: ci-repo
- name: ci-project
  path: ci-project/

run:
  # path: ci-repo/concourse/task_run_tests.sh
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    ls -al
    cd ci-repo/concourse/
    ls -al
    chmod +x task_run_tests.sh
    ls -al
    ./task_run_tests.sh
